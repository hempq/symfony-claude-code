---
description: Create a Symfony controller (alias for controller-new)
model: claude-sonnet-4-5
---

This is an alias for `/controller-new`. Use `/controller-new` for full documentation.

Create a simple Symfony controller.

## Controller Specification

$ARGUMENTS

## Quick Controller Patterns

- **Simple Controller** - Basic routes and templates
- **CRUD Controller** - Create, Read, Update, Delete
- **API Controller** - JSON responses
- **Form Controller** - Form handling with validation
- **Dashboard Controller** - Admin dashboard

For full controller command documentation, use `/controller-new` or see `.claude/commands/ui/controller-new.md`.
