---
description: Create a Symfony service (alias for service-new)
model: claude-sonnet-4-5
---

This is an alias for `/service-new`. Use `/service-new` for full documentation.

Create a Symfony service with business logic.

## Service Specification

$ARGUMENTS

## Quick Service Patterns

- **Simple Service** - Business logic with dependency injection
- **Service with External API** - HTTP client integration
- **Service with Caching** - Cache integration
- **Service with Events** - Event dispatcher
- **Service with File Upload** - File handling
- **Service with Email** - Mailer integration

For full service command documentation, use `/service-new` or see `.claude/commands/misc/service-new.md`.
